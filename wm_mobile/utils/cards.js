/* import { cardModel } from "./cardModel"

export const fillDeskCard = async (data) => {    
    Object.keys(cardModel.desk).forEach(p => {
        data[p].value = result[p]
    })
    const form = new myForm()
    return form.render(data)
    //console.log(data)
} */